//for each loop in array METHOD
//function can be passed as Parameter in JS
const arr = [1, 2, 3];
arr.forEach(function printval(val){ //used for each arrays when they have to do work
    console.log(`Value is ${val}`);
});